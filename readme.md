# CharlestonCodes Site


## Development Setup

* Install NodeJs
* Install Bam

```
npm install bam -g
```

layout.html is the layout page
all templates are in coffeecup in the pages folder

To run in development mode:

```
bam run
```

To generate static files:

``` 
bam gen
```

